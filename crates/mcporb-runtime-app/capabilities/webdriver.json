{
  "identifier": "webdriver",
  "description": "E2E-only capability for the embedded WebDriver server (webdriver feature). Referenced by tauri.conf.json app.security.capabilities; MAS builds override the list to [default] via TAURI_CONFIG so this file is never enabled in production.",
  "windows": ["main"],
  "permissions": [
    "wdio-webdriver:default"
  ]
}
